{
  "private": true,
  "scripts": {
    "web": "yarn workspace tailwind-example expo start --web",
    "postinstall": "expo-yarn-workspaces postinstall"
  },
  "workspaces": [
    "example",
    "packages/*"
  ],
  "devDependencies": {
    "expo-yarn-workspaces": "^1.5.0"
  }
}
