{
  "private": true,
  "scripts": {
    "start": "cd example && yarn start",
    "build": "cd library && yarn build --watch"
  },
  "workspaces": [
    "example",
    "library"
  ],
  "devDependencies": {
    "expo-yarn-workspaces": "^1.5.0"
  }
}
